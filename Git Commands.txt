Git & GitHub -> https://www.youtube.com/watch?v=SWYqp7iY_Tc

BASIC COMMANDS
- $ git init // Initialize Local Git Repository
- $ git add <file> // Add File(s) To Index
- $ git status // Check Status of Working Tree
- $ git commit // Commit Changes In Index
- $ git push // Push To Remote Repository
- $ git pull // Pull Latest From Remote Repository
- $ git clone // Clone Repository Into A New Directory

INSTALLING GIT
Windows -> http://git-scm.com/download/win

Commands
$ git --version // to check version of git
$ touch <fileName>.<fileType> // create file
$ git init // Initialize Local Git Repository
$ git config --global user.name 'Dev Dan'
$ git config --global user.email 'tremochaniel@gmail.com'
$ git add <file> // Add File(s) To Index
$ git status // Check Status of Working Tree
$ git rm --cached index.html
$ git add *.html // Add All HTML File(s) To Index
$ git add . // Add All File(s) To Index
$ git commit
# click 'i' to insert
# remove '#' to the 'Init Commit'
# click 'esc' then ':wq' to exit
$ git commit -m 'Changed add.js' // skip all that process
$ touch .gitignore
$ git branch <name> // create branch
$ git commit -m 'another change' // skip all that 
$ git checkout <nameOfTheBranch> // Switch branch
$ git merge login
$ git remote // check origin
$ git remote add origin https://github.com/DevDanGTM/myappsample.git // add origin
$ git push -u origin master
$ touch README.md
$ git add .
$ git push // push it through gitHub


TO GET FILES:
$ git clone <codeLink>
            https://github.com/DevDanGTM/myappsample.git

TO GET CHANGES:
$ git pull.